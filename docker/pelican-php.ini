; This is the maximum size of a file that can be uploaded. This is important for livewire based uploads.
; This can not be increased by the user, so it should ideally be higher than a user would ever encounter, but due to security concerns it is limited to 100MB
upload_max_filesize = 100M
; See reason given for upload_max_filesize
post_max_size = 100M
